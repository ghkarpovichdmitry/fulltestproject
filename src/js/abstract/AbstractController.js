export default class AbstractController {
  constructor (controller) {
    this.controller = controller;
  }

  init () {
    // this.view.init();
    // this.setSelectors();
    // this.setEvents();
  }
}
